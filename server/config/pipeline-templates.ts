// Re-export from shared module for backward compatibility
export {
  PIPELINE_TEMPLATES,
  getTemplateById,
  getTemplatesByCategory,
  formatCategoryLabel,
  type PipelineTemplate,
} from '@shared/pipeline-templates';
